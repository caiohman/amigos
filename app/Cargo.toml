[package]
name = "app"
version = "0.1.0"
edition = "2024"

[dependencies]
mysql = { version = "*", features = ["chrono"] }
chrono = "0.4"
gtk = { version = "0.11.3", package = "gtk4", features = ["v4_22"] }

[build-dependencies]
glib-build-tools = "0.22.0"

